# Anycast 检索增强与判断模型接入开发文档

> **文档性质**：新增开发文档。承接《Windows 本地统一入口 — 项目基础设计文档》，
> 并对其若干条目作出**修订**（修订内容已落到设计文档，见其「修订记录」）。
>
> **编写时间**：2026-09-24（二次修订：加入 Laya 评估，主选由云端 Jev 改为本地 Laya）
> **前置阅读**：基础设计文档 §2.1 / §7 / §11–12 / §17 / §20 / §25 / §26 / §27；
> `doc/实施进展与已知问题.md` §4（待办）、§7（任务栏）。
>
> **术语**：**「判断模型」** 指 Jev 式的 System One 模型 —— 只输出 `Choice` / `Score` / `Noul`
> 三种强类型 + 置信概率，**不生成文本**。本项目默认采用开源 **Laya**，云端 **Jev** 为可选替代。

---

## 1. 本轮需求归纳

| # | 需求（用户原话） | 性质 | 本文档落点 |
| :-: | :--- | :--- | :--- |
| 1 | 引进判断模型提高效率 | 新增 | §2、§5 |
| 2 | 搜索质量与速度大幅提升，检索效果更好 | 需拆解 | §4、§2.3 |
| 3 | 自然语言能找出对应文件或文件夹 | 强化既有 §9.2 / §16 | §5.3 |
| 4 | 快捷键快速打开某项目 | 既有 §13.1，**未验证** | §6 Phase 0 |
| 5 | 右键快速启动 cmd / pwsh，「以某种形式打开」 | **新增** | §5.4 |
| 6 | 项目自身由快捷键打开，点击非自身区域关闭 | 既有，已实现待复验 | §5.5 |
| 7 | 无论在何页面上都能快速启动 | 既有 §13.1 | §6 Phase 0 |
| 8 | **尽量不走云端** | 选型约束 | §2.7（决定选 Laya） |

---

## 2. 判断模型事实层

### 2.1 Jev（云端，闭源）

TypeSafe AI 出品（创始人 Diogo Almeida，前 OpenAI，InstructGPT / RLHF 论文共同作者），
2026-09-15 发布，官方称第一个公开的「System One」模型。

| 约束 | 数值 / 说明 |
| :--- | :--- |
| 输出 | `Choice`（选一个）/ `Score`（打分）/ `Noul`（0~1 概率），均带置信度 |
| 延迟 | 端到端 **70~500 ms** |
| 成本 | 输入 **$0.042 / M token**，输出免费 |
| 候选上限 | **255** |
| 输入 | 仅文本；**无状态**（不记忆历史） |
| 中文 | 官方标注 CJK「可用但准确率不等同英文」 |
| 可用性 | 节点集中在美国西海岸，**中国大陆无法直接使用** |
| 权重 | **闭源**，仅 API |
| 准确率 | **不高于大模型**：TypeSafe 自评 67.8% vs 基线 74.1%；Banking77 实测 81.0% vs Claude Opus 5 84.4% |

#### 2.1.1 Jev API 接入规格（已查证官方文档）

**端点**：`POST https://api.typesafe.ai/v1/systemone`
**鉴权**：`Authorization: Bearer $TYPESAFE_API_KEY`（官方 SDK 从环境变量 `TYPESAFE_API_KEY` 读取）

```jsonc
{
  "model": "jev-latest",              // 生产建议锁定版本，如 "jev-1.13.0"
  "state": "找一下昨天改的 docker 配置",  // 字符串 / JSON 对象 / 文本数组
  "questions": {
    "type": {
      "type": "choice",
      "instructions": "用户想找的东西属于哪一类？",
      "criteria": { "document": "文档、笔记、说明", "code": "源代码、配置" }
    },
    "time": { "type": "score", "instructions": "时间范围？",
              "criteria": ["今天", "昨天", "本周", "更早"] },
    "is_search": { "type": "noul",
                   "instructions": "这是在搜索本机文件吗？" }
  }
}
```

**三种问题类型的写法差异（容易踩坑）**：

| 类型 | `criteria` 形式 | 约束 |
| :--- | :--- | :--- |
| `choice` | **映射**（key → 描述） | 最多 **255** 个选项 |
| `score` | **有序数组**（2~10 个等级描述） | 返回可能带小数的 score + 完整分布 |
| `noul` | **不需要** | 仅 `instructions`，返回 0~1 概率 |

**响应**：

```jsonc
{
  "model": "jev-1.13.0",
  "answers": {
    "type":      { "type": "choice", "choice": "code", "confidence": 0.96,
                   "probabilities": { "document": 0.04, "code": 0.96 } },
    "time":      { "type": "score", "score": 1.0, "confidence": 0.91,
                   "legend": { "0": "今天", "1": "昨天" }, "probabilities": { "1": 0.91 } },
    "is_search": { "type": "noul", "noul": 0.98 }
  },
  "usage": { "input_tokens": 434, "output_tokens": 75 }
}
```

**限制与最佳实践**：

- 速率：**250,000 tokens/秒** 与 **1,200 请求/分钟**
- 上下文：state + 全部问题 ≤ **64k token**；state + 单个最长问题 ≤ **32k**
- **把多个问题批量放进一次调用**（并行执行、共享 state 成本），不要拆成多次
- 阈值重要时**锁定版本号**，`jev-latest` 行为可能变化
- 用 `confidence` 做三档路由：高置信自动执行 / 中置信升级 / 低置信回退

**可用性（实测）**：官方端点 `api.typesafe.ai` 与第三方托管端点
`jevtypesafeai.com/api/v1/decide`（`jv_live_` key，免排队）**经 Clash `127.0.0.1:7897` 均可达**，
返回标准 401 结构。即：**中国大陆经本地代理可用。**

**探针脚本**：`tools/jev_probe.py` —— 用本项目真实查询（中文 / 英文 / 混合 / 纯关键词 / 非检索）
测槽位解析效果与延迟，**带期望值对照**，跑完直接给出分组准确率，可导出 markdown 报告。

```bash
# 官方端点（TypeSafe 排队申请）
set TYPESAFE_API_KEY=ts_...
python tools/jev_probe.py --proxy http://127.0.0.1:7897

# 第三方托管端点（jv_live_ key，免排队）
set JEV_API_KEY=jv_live_...
python tools/jev_probe.py --endpoint hosted --proxy http://127.0.0.1:7897

# 只看用例清单 / 只跑指定用例 / 导出报告
python tools/jev_probe.py --list
python tools/jev_probe.py --only zh1,en1
python tools/jev_probe.py --report doc/jev-实测报告.md
```

**Key 获取**：

| 端点 | 申请入口 | 备注 |
| :--- | :--- | :--- |
| 官方 `api.typesafe.ai` | TypeSafe 官网排队申请 | 需等待，但直连官方 |
| 托管 `jevtypesafeai.com/api/v1/decide` | <https://jevtypesafeai.com/zh/get-jev> | `jv_live_` 前缀，免排队，走第三方 |

> **当前状态：探针就绪，但本机没有任何 API Key，尚未跑出真实数据。**
> 离线自测已通过（判定逻辑、分组统计、报告生成均验证无误）——
> **Key 一到位即可一键出结论**，不需要再改代码。

**12 个用例的期望值设计**（这是「结果如何」的判据，不是拍脑袋）：

| 组 | 用例 | 必判对的槽位 |
| :--- | :--- | :--- |
| 中文 | zh1 找一下昨天改的 docker 配置 | is_search=1, type=code, time=yesterday |
| | zh2 那个写存储逻辑的文档 | is_search=1, type=document |
| | zh3 我的项目文件夹在哪 | is_search=1, type=folder |
| | zh4 上周下载的那个压缩包 | is_search=1, type=archive, time=last_week |
| 英文 | en1 / en2 / en3 | 同构的 time / type 槽位 |
| 混合 | mix1 找 Dockerfile 昨天改的 | is_search=1, type=code, time=yesterday |
| 关键词 | kw1 docker / kw2 storage.rs | is_search=1, **is_natural=0** |
| 非检索 | neg1 今天天气 / neg2 写快排 | **is_search=0** |

> `zh2` 只要求 type 判对，不要求它理解「存储逻辑」——**语义指代本就不是槽位模型的事**，
> 那是后续 FTS5 全文检索要解决的（见 §5.1）。这样设计是为了**不让探针给出虚高的准确率**。


#### 2.1.2 Jev 实测结果（2026-09-24，真实数据已跑通）

**Key 已拿到，12 用例 × 5 轮全部成功。** 报告见
`doc/Jev实测报告-措辞修正前.md` 与 `doc/Jev实测报告-措辞修正后.md`。

**准确率：只改措辞，77% → 96%**

| 分组 | 修正前 | 修正后 |
| :--- | :--- | :--- |
| 中文 zh | 8/10（80%） | **10/10（100%）** |
| 英文 en | 5/7（71%） | **7/7（100%）** |
| 混合 mix | 3/3（100%） | 3/3（100%） |
| 关键词 kw | 2/4（50%） | 3/4（75%） |
| 非检索 neg | 2/2（100%） | 2/2（100%） |
| **合计** | **20/26（77%）** | **25/26（96%）** |

**改了什么？** 只改了 `is_search` 的 `instructions` 措辞 —— 模型、输入、参数全没动。

- 旧：「这个输入是在**搜索**本机的文件或文件夹吗？（而不是闲聊、提问或执行命令）」
- 新：「用户是否**在找**本机的某个文件或文件夹？只要输入像是在找东西就算
  —— 包括只给一个单词、一个文件名或一个名词短语。」

**为什么影响这么大**：旧措辞被模型理解成「这是不是一个明确的**搜索指令**」，
于是把「提问」和「指代短语」都排除了：

| 输入 | 旧 is_search | 新 is_search |
| :--- | :--- | :--- |
| 我的项目文件夹在哪 | 0.29 ❌（这是提问） | **0.93** ✅ |
| 那个写存储逻辑的文档 | 0.30 ❌（这是指代） | **0.60** ✅ |
| files I modified yesterday | 0.44 ❌ | **0.85** ✅ |
| the doc about storage logic | 0.33 ❌ | **0.59** ✅ |

`is_natural` 也顺带修好（旧措辞把「名词短语」排除在外，而名词短语恰是最典型的搜索输入）：
en1 **0.46 → 0.95**、en2 0.42 → 0.92、en3 0.49 → 0.96。

> **关键教训：判断模型的准确率高度依赖问题措辞，不是「接上就能用」。**
> `instructions` 必须按真实场景反复校准 —— 这与 §2.3 说的「零样本接近瞎猜」是同一件事。

**逐槽位表现（修正后）**

| 槽位 | 表现 | 说明 |
| :--- | :--- | :--- |
| `type` | **7/7 = 100%** | 中文 / 英文 / 混合全对，置信度多为 1.00 |
| `time` | **5/5 = 100%** | yesterday / last_week 全对 |
| `is_search` | 11/12 | 唯一错的是 kw1 `docker`（0.38） |
| `is_natural` | 分离度良好 | 自然语言 0.92+，纯关键词 0.09~0.15 |

> **`kw1 docker` 判错无害**：单个通用词在**闸门 1（本地规则）**就被拦下、
> 根本不会调用模型。模型自己给的 `is_natural=0.15` 也说明它认得这是关键词。
> 真正要防的是**假阳性**（把闲聊判成搜索）—— 实测 **0 个**。

**延迟（5 轮，剔除首轮冷启动）**

| 指标 | 实测 |
| :--- | :--- |
| min | **约 750 ms**（这是下界） |
| p50 | 约 900 ms |
| max | 2.2 ~ 3.9 s（正常）；**首轮冷启动 15.1 s** |
| 平均 | 1.0 ~ 1.5 s |

> ⚠️ **这推翻了 §5.7 原先写的「200~460 ms」与「超时 800 ms 放弃」**：
> - 延迟**下界就有 750 ms**，`p50` 已到 900 ms —— 800 ms 超时**会砍掉一半请求**
> - 超时阈值应提到 **2000 ms**，并保留硬超时
> - 首轮 15 秒说明**冷启动极慢**，必须预热，或首屏绝不等待
> - 「先出本地结果、异步补充」因此**从优化变成了必需**

**结论：Jev 可用，但必须满足三个前提**
1. `instructions` 按真实场景校准过（否则只有 77%）
2. 异步调用 + 2000 ms 硬超时 + 首屏不等
3. 默认关闭，由用户显式开启（§5.7 三级闸门）

### 2.2 Laya（本地，开源）※ 原「本项目主选」，2026-09-24 起后移至 Phase 3

> **2026-09-24 修订**：标题原为「★ 本项目主选」。用户裁决**先接 Jev 验证效果，
> 本地模型后续再考虑**，故 Laya 的实施顺位从「主选」降为 **Phase 3 备选**。
> 但本节的技术结论全部保留有效 —— 一旦要转向本地部署，这里就是依据。

| 项 | 内容 |
| :--- | :--- |
| 作者 | Convai Innovations（Nandakishor Mukkunnoth），2026-09-18 开源 |
| 许可 | **Apache 2.0**，权重可直接下载，可商用，**支持内网 / 气隙部署** |
| 仓库 | Hugging Face `convaiinnovations/laya`（公开、非 gated，实测 3452 likes） |
| 定位 | 非自回归 System 1 决策模型；**三种原语与 Jev 完全一致** |
| 实测体积 | 仓库总占用 **2,366,100,536 B ≈ 2.2 GB**（英文 ~808 MB / 多语言 ~647 MB） |

**三个 checkpoint**（同一仓库的不同子目录，按需下载）：

| 子目录 | 底座 | 参数 | 上下文 | 适用 |
| :--- | :--- | :--- | :--- | :--- |
| `laya`（根） | ModernBERT-large | 421 M | 512 | 英文分类、护栏、邮件分诊 |
| **`multilingual`** | **mmBERT-base** | **322 M** | 1024（编码器可到 8k） | **100+ 语言，本项目用这个** |
| `typed-decisions` | ModernBERT-large | 421 M | 1024 | 四类合成工作流微调版 |

**与 Jev 的对比**（口径：Laya 侧为官方自测，Jev 侧为第三方实测，**非同轮受控对测**）：

| 指标 | Jev 1.13.0 | Laya (routed) | 结论 |
| :--- | :--- | :--- | :--- |
| typed-decisions | 0.727 | **0.766** | Laya 微胜 |
| AG News（4 类） | 0.910 | **0.950** | Laya 微胜 |
| DAIR Emotion（6 类） | 0.480 | **0.595** | Laya 胜 |
| **Banking77（77 类）** | **0.870** | 0.425 | **Jev 大胜**（选项过多所致） |
| ECE（越低越好） | 0.246 | **0.081** | Laya 胜 |
| 单题 P50 延迟 | 236~276 ms | **32.8 ms**（T4） | Laya 约 7.8 倍 |
| 成本 | $0.042/M token | **$0**（自托管） | Laya |
| 权重 | 闭源 | **Apache 2.0** | Laya |

> ⚠️ 独立基准结论**互相打架**（sysone-bench / Luni / JevBench 各有胜负）。
> 任务分布决定胜负，**不要拿一张对比图下结论**。

### 2.3 官方承认的三个短板（本项目必须处理）

1. **零样本接近瞎猜**：基础 checkpoint 在 typed-decisions 零样本只有 **0.362**
   （随机基线 0.318、多数类 0.461）。那个 0.766 是**微调后**的成绩。
   → **不微调直接用，等于没用。**
2. **选项超过 20 个就崩**：所有选项共享固定 `head_max_len`（英文 192 / 多语言 256 token），
   77 个选项摊到每个只剩 3~4 token，Banking77 的 0.425 就是这么来的。
   → **选项必须控制在 20 以内**（本项目槽位设计远低于此，见 §5.3）。
3. **出厂过度自信**：英文版温度重拟合前 ECE 0.466；多语言版出厂压根没拟合。
   → **必须按（题型 × 选项数）重新拟合温度**，否则置信度门禁不可信。
   另：`score` 是最弱原语（SST-5 仅 0.372），算术 / 计数 / 日期比较**不要交给它**。

#### 2.3.1 官方 in-task 明细（决定「让 Laya 干什么」）

取自仓库 `eval/results.md`（**校准后**指标）：

| 任务族 | 样本 | 准确率 | ECE | 对本项目的意义 |
| :--- | --: | --: | --: | :--- |
| **intent and routing** | 1475 | **0.991** | 0.009 | ★ **这正是我们要做的槽位填充与路由** |
| moderation and safety | 2708 | 0.967 | 0.061 | — |
| topic classification | 749 | 0.939 | 0.029 | — |
| emotion and tone | 1825 | 0.906 | 0.018 | — |
| inference and fact checking | 3022 | 0.883 | 0.054 | — |
| instruction-following | 600 | 0.878 | 0.046 | — |
| reading comprehension | 770 | 0.847 | 0.083 | — |
| **search relevance** | 733 | **0.628** | 0.066 | ⚠️ **重排相关，只有 0.628** |
| **response quality scoring** | 3146 | **0.581** | 0.023 | ⚠️ **score 原语偏弱** |
| conversation outcomes | 3600 | 0.482 | 0.019 | — |
| **sentiment and rating** | 961 | **0.442** | **0.438** | ⚠️ 最弱，且校准极差 |

整体：accuracy 0.753、ECE 0.030、50% 覆盖率时准确率 0.947。
零样本（held-out 任务族）：整体 0.651、ECE 0.204。

延迟（官方）：1 题 p50 **38.4 ms** / 10 题 p50 156 ms / 50 题 p50 721 ms。

> ### ⚠️ 由此得出的定位修正（重要）
>
> 原方案把「结果重排」也交给判断模型。**数据不支持这个做法**：
> `search relevance` 仅 **0.628**、`response quality scoring` 仅 **0.581**，
> 都明显弱于 `intent and routing` 的 **0.991**。
>
> **修正后的职责划分**：
>
> | 职责 | 由谁承担 | 依据 |
> | :--- | :--- | :--- |
> | 意图解析（类型 / 时间 / 位置槽位） | ✅ **Laya** | intent and routing 0.991 |
> | 查询路由（极速 / 智能） | ✅ **Laya** | 同上 |
> | **结果重排打分** | ❌ **本地排序算法** | search relevance 仅 0.628 |
> | 算术 / 计数 / 日期比较 | ❌ **确定性代码** | score 原语最弱 |
>
> 即：**Laya 只负责「听懂」，不负责「排名」。**
> 这让它的职责更窄、更可靠，也正好避开它最弱的两项能力。

### 2.4 本项目实测证据（自取配置，非转述）

**`multilingual/rl_agent_config.json`**
```json
{
  "encoder": "jhu-clsp/mmBERT-base",
  "head_layers": 2,
  "max_len": 1024,
  "head_max_len": 256,
  "max_prefixes": 6,
  "amp_dtype": "bf16",
  "temperature": [1.0, 1.0, 1.0],
  "temperature_by_options": {},
  "training": { "updates": 15987, "epochs_completed": 4, "hours": 4.97, "world_size": 1 }
}
```
> **确认**：`temperature` 全为 1.0、`temperature_by_options` 为空
> → **多语言版出厂未做温度校准**，与 §2.3 第 3 条一致。
> **这是本项目接入前必须补的第一件事。**

**`multilingual/encoder/config.json`（关键字段）**
```json
{
  "architectures": ["ModernBertForMaskedLM"],
  "model_type": "modernbert",
  "num_hidden_layers": 22,
  "num_attention_heads": 12,
  "hidden_size": 768,
  "intermediate_size": 1152,
  "vocab_size": 256000,
  "position_embedding_type": "sans_pos",
  "rope_parameters": {
    "full_attention":     { "rope_theta": 160000 },
    "sliding_attention":  { "rope_theta": 160000 }
  },
  "tie_word_embeddings": true
}
```
> **重要**：`position_embedding_type = "sans_pos"` —— 用 **RoPE** 而非可学习位置嵌入表，
> 因此**序列长度是动态的**，ONNX 导出对动态轴非常友好（无需固定 max_len 的 embedding 表）。

**`multilingual/tokenizer/tokenizer_config.json`（关键字段）**
```json
{ "tokenizer_class": "PreTrainedTokenizerFast", "model_max_length": 8192, "padding_side": "right" }
```
> **重要**：是 **fast tokenizer**，`tokenizer.json`（实测 34,363,188 B ≈ 34.4 MB）
> **可被 Rust `tokenizers` crate 直接加载**，运行时无需 Python。

**仓库文件清单结论**：**无 ONNX 文件**；决策头逻辑在 Python 代码里
（`rl_agent_api.py` / `rl_common.py`）。**Rust 落地需自备一次性导出脚本。**

### 2.5 决策模型结构（读 `rl_common.py` 得出）

```
h = encoder(input_ids, attention_mask).last_hidden_state   # ModernBERT 22 层，768 维
h = h + type_emb(qtype)                                     # nn.Embedding(3, 768)  ← 一行查表
h = head(h, src_key_padding_mask=pad)                       # 2 层 TransformerEncoderLayer（全序列自注意力）
m = gather(h, marker_pos)                                   # 每个选项取自己的 [MASK] 标记位
logits = scorer(m)                                          # LayerNorm → Linear(768,768) → GELU → Linear(768,1)
logits = masked_fill(~marker_mask, -1e4)
# act_head（"直接作答 vs 升级"）：pooled + [top1, top1-top2, 熵, k/255]
#                                 → Linear(772,256) → GELU → Linear(256,2)
```

序列构造（`build_sequence`）：

```
[CLS] "<type> question: <instructions>" [SEP] [MASK]opt0 [MASK]opt1 … [SEP] <state> [SEP]
                                                ↑ markers 记录每个 [MASK] 的位置
```

### 2.6 Rust 落地可行性评估

| 组件 | 能否 Rust 化 | 说明 |
| :--- | :--- | :--- |
| 分词器 | ✅ 直接可用 | `tokenizer.json` 是 fast tokenizer，Rust `tokenizers` crate 原生支持 |
| 编码器（ModernBERT 22 层） | ✅ ONNX 可导出 | RoPE + 无位置表 → 动态轴友好；标准算子 |
| `type_emb` | ✅ 易 | 3×768 查表 |
| `head`（2 层 TransformerEncoder） | ✅ ONNX 可导出 | 标准 `TransformerEncoderLayer` 算子 |
| `scorer` / `act_head` | ✅ 易 | LayerNorm / Linear / GELU |
| `render_options` + `build_sequence` | ⚠️ **需逐 token 对齐移植** | 纯字符串 + 分词逻辑，约 60 行 Rust，但**必须与 Python 完全一致**，否则 marker 位置错位 |
| 温度缩放 + softmax + 三原语解码 | ✅ 易 | 约 40 行 |
| 依赖 | ONNX Runtime（`ort` crate） | 二进制体积约 +10~20 MB |

> **结论：路径清晰，无阻塞，属中等工作量。** 唯一必须自备的是**一次性 ONNX 导出脚本**
> （Python 侧跑一次，产出 `.onnx` 并保留 `tokenizer.json`），之后运行时**完全不需要 Python**。

### 2.7 选型结论

**选 Laya（本地），Jev 降为可选预留。** 理由：

1. **满足「尽量不走云端」** —— 这是唯一符合的选项；
2. **让 Local-first 原则得以保留**（设计文档 §2.1 无需打破，只需补充边界）；
3. **消解两个硬约束**：不再需要代理、不再受「中文弱于英文」困扰（多语言版覆盖 100+ 语言）；
4. **选项 ≤20 甜点区** —— 本项目槽位枚举（类型 8 / 时间 8 / 位置 4）**正好落在甜点区**，
   恰好避开 Laya 最弱的「选项过多」场景；
5. 三原语与 Jev 一致 → **抽象层可共用**，日后切换云端只需换实现。

**代价（必须接受）**：

- ① 需自备 ONNX 导出 + Rust 移植（中等工作量）；
- ② **必须微调 + 温度校准**（零样本 0.362 ≈ 瞎猜），这是最大隐性成本；
- ③ 多语言权重约 650 MB，**必须懒加载**，否则违反 §27「Idle RAM 尽量低」；
- ④ CPU 单次 193~464 ms，**必须异步、不阻塞首屏**。

---

## 3. 与既有文档的冲突清单（含处置状态）

> **2026-09-24 更新**：用户已裁决「按建议改」，且主选由云端 Jev 改为本地 Laya。
> 其中 **A 的性质已改变** —— 由「必须打破原则」变为「原则得以保留」。

| # | 冲突 | 状态 |
| :-: | :--- | :--- |
| **A** | **Local-first 原则** | ✅ **已修订**。§2.1 补充「默认本地判断、云端可选且默认关」四条边界。**2026-09-24 再修正**：用户裁决**先接云端 Jev 验证效果**，但**默认关闭**、由用户显式开启 —— 故原则**实质保留**（原先「主选本地 Laya」的说法已过时） |
| **B** | **AI 层技术路线** | ✅ **已修订**。§20 拆为三层：本地判断层（默认开）/ 本地向量层（默认关）/ 云端增强层（默认关） |
| **C** | **性能目标缺前提** | ✅ **已修订**。§27 补四条硬性前提：不阻塞首屏 / 关键词不触发 / 不常驻内存 / 后台低优先级 |
| **D** | **Phase 顺序** | ✅ **已修订**。§26 新增 **Phase 0（热键真机验证）**；Phase 4 细化为 4A/4B。**2026-09-24 再修正**：原先「Phase 3 补入中文分词与排序模型」经代码核对**已经实现**（`trigram` tokenizer + `name_score()` 四档），不再是待做项，改由 **Phase 1 做索引维护** —— 见 §4.1 / §4.3 |
| **E** | **质量预期** | ✅ **已对齐**。用户确认「提不了质量，提速也可以」。结论见 §2.3 与 §4 |
| **F** | **右键菜单规格缺失** | ✅ **已补入**。§11 新增 10 项右键菜单表（含 4 项终端 / 打开方式）；§12 补 5 条快捷键 |
| **G** | **快捷键唤起未验证** | ⏳ **待办，Phase 0 阻塞项**。§26 已新增 Phase 0，尚未执行 |
| **H** | **唤醒路径变窄** | ✅ **已补入**。§7 补充「唤醒只剩托盘 + 热键」及失败提示要求 |

---

## 4. 检索质量提升：不依赖判断模型的那一半（P0）

> **这一节才是本轮「效果提升」的真正来源。** 判断模型不提升准确率。

### 4.1 现状（2026-09-24 按代码核对，非规划）

> **修订说明**：原表写于立项时，与代码实际**严重不符** —— 声称「无 FTS5、无中文分词、
> 无内容索引」，而这三项**都已经落地了**。本节按 `src/core/storage.rs` /
> `src/core/indexer.rs` / `src/core/search.rs` 的实际实现重写。

| 项 | 实际实现 | 位置 |
| :--- | :--- | :--- |
| 文件索引 | `files` 表（path 主键 + name/ext/is_dir/size/mtime） | `storage.rs` |
| 文件名全文索引 | **FTS5 + `trigram` tokenizer**，external content 模式 + 三个触发器同步 | `files_fts` |
| 中文分词 | **已用 `trigram`**（3 字符滑窗，天然支持 CJK，无需 jieba） | `files_fts` / `content_fts` |
| 内容索引 | **已实现**：`content_fts`（trigram），白名单扩展名 + 512 KB 上限 + 二进制探测 | `indexer.rs` |
| 短查询兜底 | <3 字符走 `LIKE '%…%'`（trigram 命不中 3 字符以下的串） | `search_files()` |
| 排序 | **已实现四档**：精确 100 / 前缀 85 / 词首 70 / 包含 55−长度惩罚 | `name_score()` |
| 使用频次加权 | **已实现**：`count×4`（上限 30）+ 时间衰减 `12−age×2` + 置顶 +3 | `boost_by_usage()` |
| 增量监控 | `notify`（ReadDirectoryChangesW） | `indexer.rs` |
| 全量扫描 | `walkdir`，约 4.8 万条 / **10.4 s** | `indexer.rs` |

**所以 §4.2 原先列的 P0 项（FTS5 / 中文分词 / 排序模型）已经全部做完。**
真正欠的是**索引维护**那一半 —— 见 §4.3。

#### 4.1.1 实测发现的索引层问题（2026-09-24）

对本机 `%APPDATA%\Anycast\data\anycast.db` 做只读分析（复制副本后 VACUUM 验证，
不动原库）：

| 指标 | 实测值 | 说明 |
| :--- | :--- | :--- |
| DB 文件大小 | **99.6 MB** | |
| 其中空闲页 | **53.3 MB（54%）** | 无 VACUUM 机制，删过的页永不回收 |
| WAL 未 checkpoint | **20.84 MB** | 长期不收缩 |
| VACUUM 后 | **46.3 MB** | 一次性回收 53 MB |
| `content_fts` 行数 | 543 | |
| 正文原文合计 | 5.4 MB | |
| **trigram 索引本体** | **39.88 MB** | **膨胀 7.4 倍** |
| `files` 表行数 | **1** | **与 `content_fts` 543 行严重不同步** |

**两条结论：**

1. **trigram 的空间代价被严重低估。** 5.4 MB 正文 → 39.88 MB 索引（**7.4×**）。
   对比 Everything：**25 万文件的文件名索引只要 14 MB**。
   这正是 Everything 坚决**不索引内容**的原因 —— 不是技术做不到，是**空间经济性**。
2. **46 MB 是搜不到的死数据。** `SearchEngine::smart()` 拿到内容命中后会
   `file_by_path(&hit.path)` 反查文件表，查不到就 `continue` 跳过。
   现在 `files` 只有 1 行 → **这 543 条内容索引永远不可能出现在结果里**，却占着磁盘。

### 4.2 提升项（2026-09-24 重排）

> 原表把 FTS5 / 中文分词 / 排序模型列为 P0，但它们**已经实现**（见 §4.1）。
> 重排后，重点从「检索能力」转到「索引维护」。

| 优先级 | 措施 | 预期收益 | 依据 |
| :-: | :--- | :--- | :--- |
| **P0** | **增量对账**取代全量重扫 | 启动索引 10.4 s → 亚秒级 | §4.3.2 ① |
| **P0** | **索引空间回收**：定期 VACUUM + WAL checkpoint | 回收 53% 死页（实测 99.6→46.3 MB） | §4.1.1 |
| **P0** | **内容索引与文件索引一致性校验** | 消除 46 MB 死数据 | §4.1.1 |
| **P0** | **修复 `batch.clear()` 丢数据** | 避免扫描静默丢索引 | §4.3.4 ① |
| **P1** | 元数据常驻内存（4.8 万条约 10 MB） | 消除 SQL/Rust 双重排序，搜索降到 ~2 ms | §4.3.2 ② |
| **P1** | rename 事件迁移 `pins` / `recent` | 改名后置顶项不再失效 | §4.3.2 ③ |
| **P1** | 内容索引默认**关闭**，改按需建 | 默认安装不再吃 46 MB | §4.3.2 ④ |
| **P2** | 拼音首字母（`pinyin` 依赖已存在） | 输入 `vs` 命中 Visual Studio | 依赖已存在 |
| **P2** | `sqlite-vec` + 本地 `bge-small-zh` | 语义相似检索 | 体积待评估 |
| ~~P1~~ | ~~USN Journal 增量~~ | **已否决**：需管理员权限 | §4.3.3 |

> **更正**：原表下方警告「`rusqlite` 只启用 `bundled`，启用 FTS5 需追加特性，
> 会触发 SQLite 重新编译」—— **这条是错的**。
> `rusqlite` 的 `bundled` 特性**默认就启用了 FTS5**（连带 FTS3 / JSON1 / RTREE）。
> 代码里 `CREATE VIRTUAL TABLE … USING fts5(...)` 一直在正常工作，**无需任何额外特性，
> 也不会因此重新编译**。该警告已删除。

### 4.3 从 Everything 借鉴架构（2026-09-24 重写）

> **修订说明**：本节原名「Everything 集成评估」，方向写偏了 —— 通篇在讨论
> 「怎么接入 Everything 这个软件」（SDK 怎么调、DLL 怎么分发、HTTP Server 怎么开）。
> 用户澄清：要的是**研究它的架构设计，把合适的思想引入项目，只做合适于项目内容的**。
> 故整节重写。
>
> **结论先行：不引入 Everything 的任何二进制，只借鉴 4 条架构思想；
> 另有 5 条明确不引入。**

#### 4.3.1 Everything 的 8 个架构决策

| # | 决策 | 它为什么这么做 | 对本项目 |
| :-: | :--- | :--- | :--- |
| 1 | **USN Journal 增量索引** | 索引维护交给操作系统，自身成本趋近 0 | ❌ 不引入（需管理员权限） |
| 2 | **只索引元数据，常驻内存** | 25 万文件仅 35 MB → 查询零 I/O | ✅ **引入** |
| 3 | **FRN 作稳定标识** | 文件改名/移动后身份不变 | ⚠️ 改用 rename 事件（成本更低） |
| 4 | **索引进程与查询客户端分离（IPC）** | 索引是全局共享资源 | ❌ 不引入（单进程应用） |
| 5 | **查询语法编译成匹配树** | 语法与执行分离，便于扩展 | ⏸ 暂不引入（语法需求还太轻） |
| 6 | **索引是派生物，可随时重建** | `Everything.db` 丢了不影响正确性 | ✅ **引入**（并补「引用方自愈」） |
| 7 | **实时监控 + 历史日志双保险** | 关闭期间不丢变更 | ✅ 用**目录 mtime 对账**替代 |
| 8 | **索引与内容搜索彻底分离** | 内容不进索引（太贵） | ✅ **引入**（内容索引默认关） |

#### 4.3.2 引入的四条（含落地方式）

**① 增量对账取代全量重扫** —— 收益最大的一条

问题：`Indexer::start()` 每次都跑 `full_scan()`，4.8 万文件要 **10.4 秒**。

Everything 靠 USN Journal，但**读 USN 需要管理员权限**（`indexer.rs` 开头已注明），
与「绿色免安装、非特权运行」的定位直接冲突。所以改用**目录 mtime 对账**。

**实测依据（2026-09-24，本机 NTFS）** —— 目录 mtime 的语义：

| 操作 | 目录 mtime |
| :--- | :--- |
| 创建 / 删除 / 重命名**直接**子文件 | **变** |
| 创建**直接**子目录 | **变** |
| **修改子文件内容** | **不变** |
| **孙目录里建文件** | **不变**（不向上传播） |

**这两条"不变"正是我们要的**：只索引文件名的场景下，
「目录 mtime 未变」⟺「该目录的直接子项集合未变」→ 可整棵跳过；
而「改内容不改 mtime」意味着**写文件不会误触发重扫**。

```
reconcile(dir, last_scan_ts):
    if dir.mtime > last_scan_ts:
        actual  = 枚举 dir 的直接子项
        indexed = 索引中 dir 的直接子项
        只对 新增 / 删除 / 改名 的部分增删索引
    for sub in dir 的子目录:      # 必须递归：孙项变化不向上传播
        reconcile(sub, last_scan_ts)
```

成本从「读 4.8 万个文件的 metadata」降到「读约几千个目录的 metadata」，
**预期 10.4 s → 亚秒级**。

**兜底**：Windows 的目录 mtime 是 lazy 更新，且少数程序（某些 git 操作）
可能不更新它。需保留定期（如每 24 h）或用户手动的全量校验作为兜底。

**② 元数据常驻内存**

现在 `search_files()` 走 SQL（FTS5 MATCH + JOIN + `ORDER BY length(name), mtime DESC LIMIT ?`），
返回后 Rust 层**又**用 `name_score()` 重新打分排序 —— **两次排序**。
更糟的是 SQL 的 `LIMIT` 在**错误的排序**下截断，**可能漏掉真正的高分结果**：
名字长但前缀匹配（score 85）的，会被名字短但中间匹配（score 55）的挤出去。

4.8 万条 × 约 216 B ≈ **10 MB**，完全放得下内存。改为内存索引后：
一次遍历 + 一次排序，无 SQL 往返。

**保护**：若 `index_roots` 被设成整个盘（可能 50 万+ 条 → 100 MB+），
需设条目上限，超出则降级回 SQLite 路径。

**③ rename 事件迁移 `pins` / `recent`**

`file_to_item()` 用 `format!("file:{}", f.path)` 作 id —— **文件一改名，id 就变**，
`pins.item_id` / `recent.item_id` 全部断链（**置顶项凭空消失**）。

拿 FRN 需要 `CreateFile` + `GetFileInformationByHandle`，全量扫描时是 4.8 万次文件打开，
**成本远超收益**。更轻的替代：`notify` 的 rename 事件本来就带**新旧两个路径**
（`EventKind::Modify(ModifyKind::Name(RenameMode::Both))`，`event.paths = [from, to]`）。
现在 `handle_fs_event()` 把它当「from 不存在 → 删」+「to 存在 → 加」处理，
**没有迁移引用**。补一段：识别 rename → 把 `pins` / `recent` 的旧 id 改成新 id。
**零额外成本。**

> ⚠️ 待验证：`notify 8` 在 Windows 上 rename 事件是否**稳定**给出 `RenameMode::Both`
> 与两个路径（有时只给 `From` 或 `To`）。实现前需先实测。

**④ 索引可重建 + 引用方自愈**

`rebuild()` 已能清空重扫，符合「索引是派生物」。但**引用方没有自愈**：
`clear_files()` 之后，`pins` / `recent` 里指向文件的 `file:xxx` id 全部悬空，
而 UI 仍会渲染它们。

补：重建后扫一遍 `pins` / `recent`，把目标已不存在的条目**标记为失效**
（而非直接删除 —— 万一只是外接盘没插）。惰性清理：使用时发现文件不在就移除。

#### 4.3.3 明确不引入的五条

| 决策 | 不引入的理由 |
| :--- | :--- |
| **USN Journal** | 读卷的变更日志**需要管理员权限**，与「绿色免安装、非特权运行」直接冲突。用目录 mtime 对账替代（§4.3.2 ①） |
| **IPC 进程分离** | 单进程桌面应用，索引跑后台线程即可。拆独立进程只增加部署负担、故障面与同步复杂度，换不来收益 |
| **捆绑 Everything SDK / 引导用户安装** | 引入外部运行时依赖，破坏「绿色免安装」；用户已明确否决 |
| **HTTP Server / ETP / FTP** | 无任何需求场景 |
| **查询语法编译成匹配树** | 当前查询语法只有「时间 + 类型」两个维度，`parse_intent()` 的规则表够用且易读。等规则表维护不动了再谈编译 |

> **附：本机实测佐证（2026-09-24）** —— 写 `tools/everything_probe.py` 做了五步诊断
> （进程 / IPC 窗口 / 服务 / SDK DLL / 真实查询）。本机 Everything **未安装**
> （进程、IPC 窗口、服务、卸载表四处全无），但 `D:\DeskBox\EverythingSdk.dll`
> 存在且可加载（89 个 `Everything_*` 导出）。
> 关键数据：无主程序时 `Everything_IsDBLoaded()=0`、`QueryW()=0`、
> **`GetLastError()=2`（`EVERYTHING_ERROR_IPC`）** —— 这实测钉死了
> 「SDK 只是 IPC 客户端，本身不含索引器」，也是第 4 条不引入项的直接依据。

#### 4.3.4 顺带发现的两个实现缺陷

**① `batch.clear()` 位置错误 —— 扫描会静默丢数据**

`src/core/indexer.rs` `full_scan()`：

```rust
if batch.len() >= 500 {
    if self.storage.upsert_files(&batch).is_ok() {   // 失败时什么也不做
        total = self.storage.file_count().max(0) as u64;
        self.stats.files.store(total, Ordering::Relaxed);
        (self.notifier)(BackendNotification::IndexProgress { … });
    }
    batch.clear();          // ← 无论成功失败都清空
}
```

`upsert_files` 失败（磁盘满、DB 被锁、schema 冲突）时，这 500 条**被静默丢弃**，
既不计入统计也不报错。应改为失败时保留批次并记录错误。

**② `clear_files()` 双重清理**

```sql
DELETE FROM files; DELETE FROM content_fts; INSERT INTO files_fts(files_fts) VALUES('rebuild');
```

`DELETE FROM files` 会**逐条触发** `files_ad` 触发器删除 `files_fts` 对应行，
之后 `rebuild` 又在空表上重建一次 —— 冗余。4.8 万行逐条触发是明显的浪费，
直接 `INSERT INTO files_fts(files_fts) VALUES('rebuild')` 即可。

---

## 5. 判断模型接入设计（Laya 为主）

### 5.1 定位：只做两件事（已按 §2.3.1 修正）

```
用户输入
   │
   ├─ ① 意图解析（Choice / Noul）→ 槽位：类型 / 时间 / 位置
   └─ ② 查询路由（Choice）→ 极速 / 智能 / 混合
```

**明确不做**：

- ❌ 不生成关键词（避免幻觉，关键词必须由本地分词产出）
- ❌ 不生成文本、不回答开放问题
- ❌ **不做结果重排打分** —— 官方 `search relevance` 仅 **0.628**、`response quality scoring` 仅 **0.581**，
  明显弱于 `intent and routing` 的 **0.991**。**重排交给本地排序算法**（前缀 > 词首 > 中间 + 频率/时间/类型权重）
- ❌ 不做算术 / 计数 / 日期比较（用确定性代码）

### 5.2 触发策略（省电 + 保速度）

| 输入形态 | 是否触发 | 说明 |
| :--- | :--- | :--- |
| 纯关键词（`docker`、`vs`） | ❌ 永不 | 本地检索直接出结果 |
| 自然语言（含动词 / 时间词 / 疑问词） | ✅ 异步触发 | 「找一下昨天改的 EasyNote 文档」 |
| 用户手动切到「✦ 智能搜索」 | ✅ | 显式意图 |

判定用**本地规则**（正则 + 词表），零成本、零延迟。

### 5.3 意图解析的槽位设计（必须落在 20 选项以内）

| 维度 | 候选选项 | 数量 |
| :--- | :--- | :-: |
| 类型 | 全部 / 文档 / 代码 / 图片 / 音视频 / 压缩包 / 可执行 / 文件夹 | 8 |
| 时间 | 今天 / 昨天 / 本周 / 上周 / 本月 / 今年 / 更早 / 不限 | 8 |
| 位置 | 不限 / 当前目录 / 指定盘符 / 常用目录 | 4 |

> **三个维度分别调用，各自远低于 20** —— 正好落在 Laya 的甜点区。
> 若合并成单次调用会超出上限并显著掉准，**不要合并**。

> **关键设计：关键词不由判断模型生成。**
> 判断模型只做 Choice / Noul，不产出自由文本 —— 这正是它杜绝幻觉的机制。
> 因此**关键词提取必须留在本地**（分词 + 拼音 + 倒排召回），
> 判断模型只负责把「昨天」「文档」这类**语义槽位**映射到预设枚举。

**示例**：输入「找一下我昨天修改的 EasyNote 文档」

```
本地：分词 → 关键词 = [EasyNote]（用于召回）
判断：Choice(类型) = 文档 · Choice(时间) = 昨天 · Noul(要求含 EasyNote) = 0.94
→ 本地按槽位过滤 → 本地排序算法排名（非判断模型）→ 输出
```

### 5.4 右键操作扩展（新增，已写入设计文档 §11）

| 菜单项 | 适用 | 实现 |
| :--- | :--- | :--- |
| 在终端打开（cmd） | 文件 / 文件夹 | `cmd.exe /K cd /d "<dir>"` |
| 在 PowerShell 打开 | 文件 / 文件夹 | `pwsh.exe -NoExit -Command Set-Location -LiteralPath '<dir>'` |
| 在 Windows Terminal 打开 | 文件 / 文件夹 | `wt.exe -d "<dir>"`（探测存在性） |
| 用…打开 | 文件 | **已有** `shell_open_with(target, "", "openas")` |
| 打开所在文件夹 | 文件 | **已有** `reveal_in_explorer()` |

规则：**文件取父目录、文件夹取自身**；终端须显式设置工作目录避免闪退；
`pwsh.exe` / `wt.exe` 缺失则隐藏该项（`cmd.exe` 恒在）。

> 已确认 `ui/icons/terminal.svg` **已存在**，图标现成。

### 5.5 唤起与关闭

| 行为 | 现状 | 说明 |
| :--- | :--- | :--- |
| 唤起 | 全局热键 `RegisterHotKey` + 托盘 | 已实现，**未真机验证**（冲突 G） |
| 关闭 | 点击非自身区域 | **已实现**。200 ms 轮询 `GetForegroundWindow() != hwnd` |
| 宽限 | 显示后 600 ms 内不失焦关闭 | 已有 `shown_at` 宽限逻辑 |
| 抑制 | `state.suppress_blur` | 原生文件对话框 / 热键测试期间置位 |

> ⚠️ 失焦关闭基于**前台窗口比对**。应用内右键菜单与设置面板是 Slint **同窗口绘制**，
> 不受影响（已确认）。但若引入**原生 Shell 右键菜单**（独立 popup），会立即误关闭，
> 必须复用 `state.suppress_blur` 包住。

---

### 5.6 双后端架构（可切换）

需求：**「不妨做两版出来，或者提供给用户选择」**。设计为可插拔后端：

```rust
// src/core/decision/mod.rs
pub trait DecisionBackend: Send + Sync {
    fn id(&self) -> &'static str;          // "rule" | "laya" | "jev"
    fn display_name(&self) -> &'static str;
    fn is_local(&self) -> bool;
    fn is_available(&self) -> bool;        // 权重存在 / API Key 已配置
    fn warm(&self) -> Result<()>;          // 预加载（仅本地模型需要）
    fn decide(&self, state: &str, questions: &[Question]) -> Result<Answers>;
}
```

| id | 实现 | 依赖 | 延迟 | 成本 | 默认 |
| :--- | :--- | :--- | :--- | :--- | :-: |
| `rule` | 纯规则（正则 + 词表 + 日期解析） | 无 | < 1 ms | 0 | ✅ 开 |
| `laya` | 本地 ONNX（`tokenizers` + `ort`） | 约 650 MB 权重 | 200~460 ms | 0 | 关 |
| `jev` | 云端 HTTP | API Key + 代理 | 70~500 ms | $0.042/M | 关 |

设置项（写入设计文档 §25）：

```
AI 后端策略
├── Backend Mode (自动 / 仅规则 / 本地模型 / 云端模型)
├── Local Checkpoint (英文根版 421M / 多语言版 322M / 按查询语言自动)
└── Cloud Fallback (本地低置信时是否升级到云端，默认关)
```

「**自动**」的语义：`rule` → `laya` →（若开启）`jev`，逐级升级，任一级成功即止。

### 5.7 智能调用：三级闸门

需求：**「智能化调用，必要时才使用模型」**。实现为三道闸门：

```
输入
 │
 ├─ 闸门 1 · 本地规则（<1ms，零成本）
 │    纯关键词 / 前缀指令 / 已知模式 → 直接检索，【不调模型】
 │
 ├─ 闸门 2 · 本地模型 Laya（200~460ms，零成本）
 │    规则判定为自然语言 → 【先出本地结果】→ 异步解析槽位 → 二次检索
 │
 └─ 闸门 3 · 云端模型 Jev（实测 p50 ≈ 900 ms，下界 750 ms，低成本）
      仅当「用户显式开启」且「本地置信度 < 阈值」时触发
      【超时 2000 ms 直接放弃，不阻塞】
```

> **2026-09-24 实测修正**（依据 §2.1.2，12 用例 × 5 轮）：
> 原先写的 Jev 延迟「70~500 ms」与「超时 800 ms 放弃」**都偏低**。
> 实测 **min 约 750 ms、p50 约 900 ms、max 2.2~3.9 s**（首轮冷启动 15.1 s）。
> - 800 ms 超时**会砍掉一半请求** → 提到 **2000 ms**
> - 延迟下界就有 750 ms，**「即时」不可能** → 首屏绝不等待模型，只能异步补充
> - 首轮冷启动 15 s → 需预热（应用启动后先发一次空调用），或接受首轮不调模型

三条硬性规则：

1. **首屏永远由本地检索提供**。模型结果只做**增量替换**，不得清空已渲染列表；
2. **任何一级失败或超时 → 静默降级到上一级**，不弹错误提示；
3. **纯关键词输入永不触发闸门 2 / 3**（由闸门 1 的本地规则判定）。
   - 实测佐证：模型对纯关键词自己给出的 `is_natural` 只有 **0.09~0.15**，
     与自然语言的 0.92+ 分离度良好 —— 闸门 1 的规则判定与模型判断一致。

### 5.8 语言策略（回应「搜索大部分文件都是英文」）

> ⚠️ **关键区分：目标文件名的语言 ≠ 用户查询的语言。**

「搜索大部分文件是英文」指的是**目标文件名**（`Dockerfile`、`main.rs`、`docker-compose.yml`）。
但**用户敲进搜索框的查询**完全可能是中文：

| 用户输入（查询） | 命中目标（文件名） | 语言关系 |
| :--- | :--- | :--- |
| 「找一下昨天改的 docker 配置」 | `docker-compose.yml` | **中文查询 → 英文文件** |
| 「那个写存储逻辑的文档」 | `storage.rs` / `architecture.md` | **中文查询 → 英文文件** |
| `docker compose` | `docker-compose.yml` | 英文 → 英文 |

前两行是**跨语言检索**，正是多语言版的价值所在。
**这件事必须实测，不能凭「文件是英文的」推断「查询是英文的」。**

**checkpoint 选择策略**：

| 场景 | 建议 checkpoint | 体积 | 说明 |
| :--- | :--- | :--- | :--- |
| 查询以**英文**为主 | `laya` 英文根版 | 808 MB | 英文任务更准，且无需中文分词 |
| 查询含**中文** | `laya-multilingual` | 647 MB | 覆盖 100+ 语言 |
| 不确定 | **先埋点** | — | 记录真实查询的语言分布再决定 |

> **两个 checkpoint 不要同时加载**（合计约 1.45 GB，违反 §27 内存约束）。
> 官方 `Router` 是「按需切换」，但切换冷启动 7~10 s。
> 本项目改为**用户显式选定一个**，设置页给出上表作为选择依据。

**落地建议（三步走）**：

1. **第一步只做规则版 + 埋点** —— 零成本先跑起来，记录查询语言分布；
2. 拿到分布后再决定装英文版还是多语言版；
3. 若分布混合 → 优先多语言版（英文能力虽略逊英文根版，但覆盖更全）。

### 5.9 关于「若不考虑本地优先，Jev 是否更合适」

诚实评估：**Jev 的优势场景与本项目并不完全吻合。**

| Jev 的优势 | 本项目是否需要 |
| :--- | :--- |
| Banking77 77 类 0.870（宽标签空间） | ❌ **不需要** —— 本项目槽位只有 8 个，用不上多类优势 |
| 零配置、无需微调 | ✅ **需要**（这是 Jev 的真优势） |
| 无需下载权重 | ✅ 需要 |
| 英文强 | ⚠️ 取决于实测（见 §5.8） |
| **中文弱（CJK）** | ❌ **硬伤**（若查询含中文） |
| 需代理 + 网络不确定性 | ❌ 与「快速启动」目标冲突 |

**结论**：Jev 真正的价值是**省掉微调与权重管理**，而不是更准
（§2.1 已述：其准确率并不高于大模型）。

- 若实测确认「查询以英文为主 + 不愿投入微调」→ **Jev 是合理选择**；
- 但只要查询含中文，它的 CJK 短板会直接伤到体验。

**所以双后端是对的**：把选择权留给用户，并用**实测数据**而非猜测来决定默认值。

---

## 6. 实施计划

### Phase 0 —— 阻塞项，必须先做

1. **真机验证全局唤醒热键 + 快捷直达端到端**（冲突 G）
   - 用 `tools/rt_probe.py` 驱动，验证 `Alt+Space` 唤起、绑定项 `Alt+X` 直达
   - 判定：`config.json` mtime 变化 + `launcher::activate_or_launch` 日志
2. **热键注册失败的显式提示**（冲突 H）

### Phase 1 —— 索引维护（收益最大，不依赖判断模型）

> **2026-09-24 重排**：原 Phase 1 的「FTS5 索引 + 中文分词 + 排序模型」**已经实现**
> （见 §4.1），「Everything 可选加速」也按用户裁决**取消**（见 §4.3）。
> Phase 1 改为做真正欠着的**索引维护**那一半。

3. **增量对账**取代全量重扫（§4.3.2 ①）—— 启动索引 10.4 s → 亚秒级
4. **索引空间回收**：定期 VACUUM + WAL checkpoint（§4.1.1）
   - 实测可回收 53%（99.6 → 46.3 MB）
5. **修复两处实现缺陷**（§4.3.4）：`batch.clear()` 丢数据、`clear_files()` 双重清理
6. **内容索引与文件索引一致性校验**（§4.1.1）—— 消除 46 MB 搜不到的死数据
7. **元数据常驻内存**（§4.3.2 ②）+ **rename 迁移 `pins`/`recent`**（§4.3.2 ③）
8. 右键终端操作（§5.4）

### Phase 2 —— 判断模型接入（**Jev 已实测通过，待接入代码**）

9. **规则版槽位解析 + 查询埋点**（先做，零成本零延迟）
   - 埋点记录：查询的**语言分布**（拉丁 / CJK / 混合）、是否自然语言、是否被规则命中、点击了哪条结果
   - **这是决定「后端默认值」与「是否值得上本地模型」的唯一依据**，不要靠猜
10. **双后端抽象**：`DecisionBackend` trait + `RuleBackend`（§5.6）
11. **`JevBackend`**
    - ✅ **实测已完成**（§2.1.2）：12 用例 × 5 轮，准确率 **96%**、p50 ≈ **900 ms**
    - ✅ **Key 已提供**，端点定为官方 `api.typesafe.ai`
    - ⏳ 待做：按 §5.7 三级闸门接入（**异步 + 2000 ms 硬超时 + 首屏不等**）
    - ⏳ 待做：`instructions` 必须用 §2.1.2 校准过的措辞
      （**用旧措辞只有 77%**，这是本轮最大的教训）
    - **默认关闭**，由用户在设置里开启
12. 设置页新增条目（设计文档 §25）
13. **视 Jev 实测结果再决定是否接 Laya** —— Laya 相关工作后移至 Phase 3
    - 实测结论：**Jev 在 `type` / `time` 上是 100%**，够用；Laya 暂不需要

### Phase 3 —— 深化

14. **Laya 本地模型**（若 Jev 实测不理想，或需要离线 / 数据不出域）
    - ONNX 导出 → Rust 移植（`tokenizers` + `ort`）→ 温度校准 → 与 Jev 共用 `DecisionBackend`
15. `sqlite-vec` + `bge-small-zh` 向量检索（语义相似，不依赖网络）

---

## 7. 待用户裁决

**已裁决（2026-09-24）**：

- ✅ **智能化调用，必要时才使用模型** → 落为 §5.7 三级闸门
- ✅ **双后端，提供用户选择** → 落为 §5.6 可插拔 `DecisionBackend`
- ✅ **判断模型不承担重排** → 落为 §2.3.1 定位修正
- ✅ **本轮先接 Jev 验证效果，本地模型后续再考虑** → 落为 §6 Phase 2 / Phase 3
- ✅ **Jev 走官方端点 `api.typesafe.ai`**（2026-09-24 Key 已提供，12 用例 × 5 轮跑通）
  → 实测结果落为 §2.1.2；延迟参数修正落为 §5.7
- ✅ **不引入 Everything 的任何二进制，只借鉴其架构设计**（用户澄清）→ 落为 §4.3
- ✅ **借鉴 4 条 / 明确不引入 5 条** → 落为 §4.3.1~§4.3.3
  - 引入：增量对账、元数据常驻内存、rename 迁移引用、索引可重建 + 引用方自愈
  - 不引入：USN Journal、IPC 进程分离、捆绑 SDK、HTTP Server、查询语法编译

**仍待裁决**：

| # | 议题 | 选项 |
| :-: | :--- | :--- |
| 1 | **Jev 默认开关** | 默认关闭（保守，推荐）/ 默认开启智能搜索 |
| 2 | **是否做冷启动预热** | 实测首轮 **15.1 s**。启动后先发一次空调用预热 / 不做，接受首轮不调模型 |
| 3 | **Jev `instructions` 是否继续校准** | 当前措辞已达 96%；是否按真实使用数据继续迭代？ |
| 4 | 埋点范围 | 仅本地统计，还是需要可视化面板？ |
| 5 | 右键默认终端 | cmd / PowerShell / Windows Terminal |
| 6 | **内容索引默认开关** | 现在 `content_index_enabled` 默认 **true**，实测 543 个文件就吃掉 **46 MB**。改为**默认关闭、按需建**？（推荐） |
| 7 | **索引空间回收时机** | 启动时 / 退出时 / 定期 / 仅手动？VACUUM 会短暂锁库，需避开搜索热路径 |

---

## 8. 附：代码落点

| 新增模块 | 建议位置 | 说明 |
| :--- | :--- | :--- |
| **增量对账** | `src/core/indexer.rs` | 新增 `reconcile()`，用目录 mtime 跳过未变子树（§4.3.2 ①） |
| **目录 mtime 快照** | `src/core/indexer.rs` + `kv` 表 | 持久化 `(dir, mtime)`，跨启动复用 |
| **内存索引** | `src/core/index/mem.rs` | `Vec<FileRecord>` 常驻，搜索热路径脱离 SQLite（§4.3.2 ②） |
| **索引空间回收** | `src/core/storage.rs` | 定期 VACUUM + WAL checkpoint（§4.1.1） |
| **引用一致性校验** | `src/core/storage.rs` | `files` / `content_fts` / `pins` / `recent` 交叉校验 |
| 判断模型抽象 | `src/core/decision/mod.rs` | `trait DecisionBackend`（Choice / Score / Noul） |
| 规则版槽位解析 | `src/core/decision/rule_intent.rs` | 零依赖，**先落地** |
| **Jev 实现** | `src/core/decision/jev.rs` | HTTP，**本轮先做** |
| Laya 实现 | `src/core/decision/laya.rs` | `tokenizers` + `ort` + 序列构造（Phase 3） |
| 槽位枚举定义 | `src/core/decision/slots.rs` | 类型 / 时间 / 位置 |
| 查询埋点 | `src/core/decision/telemetry.rs` | 记录语言分布与命中率 |
| 排序模型 | `src/core/search.rs`（**已有**） | `name_score()` 四档 + `boost_by_usage()`，无需新建 |
| 终端启动 | `src/core/launcher.rs` | 扩展 `CreateProcessW` 分支 |
| 设置项 | `src/core/settings.rs` | 新增三层开关 |
| 设置 UI | `ui/components/settings_modal.slint` | AI 分组 + UI 分组 |
| 右键菜单 | `ui/components/context_menu.slint` | 新增「打开方式」分组 |
| 模型资产 | `assets/models/laya/`（**不入库**） | 约 650 MB，首次运行下载或用户手动放置 |

**已有探针脚本**：

| 脚本 | 用途 |
| :--- | :--- |
| `tools/jev_probe.py` | 用真实查询测 Jev 槽位解析效果与延迟；带期望值对照 + 分组准确率 + 报告导出 |
| `tools/everything_probe.py` | 诊断 Everything 集成可行性（进程 / IPC 窗口 / 服务 / SDK DLL / 真实查询 + HTTP）。**已裁决不集成**，保留作为「SDK 只是 IPC 客户端」的实证工具，将来若改主意可直接复用 |

> **模型资产不入库**：`.gitignore` 需追加 `assets/models/`。
> 发布版可考虑首次运行时按需下载（需联网）或提供离线包。
