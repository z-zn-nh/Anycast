# ── 构建产物 ─────────────────────────────────────────────
/target

# ── 临时探测 / 调试产物（非项目资产）─────────────────────
_probe_*.html
*.pdb
t18.png

# ── 本机 AI 助手工作记忆 ────────────────────────────────
# 含本机路径、硬件与个人环境信息，不入库。
# 工程结论已沉淀到 doc/实施进展与已知问题.md。
.workbuddy-ai/

# ── 本地模型资产 ────────────────────────────────────────
# Laya 多语言权重约 650MB，不入库；由首次运行下载或用户手动放置。
assets/models/
*.onnx

# ── 编辑器 / 系统 ───────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini

# ── Python（tools/ 下的探针脚本）────────────────────────
__pycache__/
*.pyc

# 构建日志（临时产物，2026-09-25 之前漏了）
build.log
